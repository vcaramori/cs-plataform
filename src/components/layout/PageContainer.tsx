// Re-export canônico — a implementação vive em src/components/ui/page-container.tsx
// Este arquivo existe apenas para não quebrar imports existentes durante a migração.
// Após todas as telas migrarem para o import correto, este arquivo será deletado.
export { PageContainer } from "@/components/ui/page-container"
